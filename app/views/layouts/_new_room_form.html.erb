<%= form_with(model: @room, remote: true) do |f| %>
  <%= f.text_field :name, autocomplete: 'off', class: 'w-40' %>
  <%= f.submit data: { disable_with: false }, class: 'cursor-pointer py-2 px-4 bg-indigo-800 text-white mx-4' %>
<% end %>
